#!/bin/bash
#!/bin/bash  -> This is a comment, She-Bang should be in line number one of a file
ls
pwd

ls -l

# Any line starting with # is not a command and it is comment. Such comment lines are ignored by interpreters